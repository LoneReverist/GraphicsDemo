#version 330 core

layout (location = 0) in vec3 vert_pos;

uniform mat4 view_transform;
uniform mat4 proj_transform;

out vec3 tex_coords;

void main()
{
	tex_coords = vert_pos;
	gl_Position = proj_transform * view_transform * vec4(vert_pos, 1.0);
}
